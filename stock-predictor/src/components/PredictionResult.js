import React from 'react';

const PredictionResult = ({ prediction }) => {
    return (
        <div>
            <p>{prediction}</p>
        </div>
    );
};

export default PredictionResult;
